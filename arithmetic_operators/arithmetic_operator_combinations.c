#include <stdio.h>
#include <stdlib.h>

/**
 * main - computes the results of arithmetic operations.
 *
 * Return: 0
 */

int main()
{
/* declare variables */
int z, x, y;

/* initialize variables */
z = 0;
x = 5;
y = 10;

/* basic combinations */
z = x + y;
printf("z: %d", z);
return (0);
}
